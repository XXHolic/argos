// import {isSupportsFetch} from '../packages/utils/src/index'
// import {isSupportsFetch} from '@thynpm/argos-utils'
// import {captureException} from '../packages/hub/src/index'
import * as argos from '../packages/browser/src/index'
import * as argos2 from './index-origin'

// argos.init({url:'eeeee'})
// argos2.init({url:'eeeee'})
const initTest = (options={}) => {
  // console.info('utils-dd',argos)
  // throw new Error('dds')
};

initTest()
